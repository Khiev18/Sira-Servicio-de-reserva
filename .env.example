# ── Variables para docker-compose.yml ──────────────────────
# Copia este archivo como .env en la raíz del proyecto y completa
# tus propios valores antes de ejecutar: docker compose up -d
#
#   cp .env.example .env

DB_PASSWORD=cambia_esta_password
JWT_SECRET=genera_un_secreto_largo_y_aleatorio_aqui
